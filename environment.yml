name: laserfarm

channels:
  - conda-forge
  - defaults

dependencies:
  - python>=3.8
  - pdal
  - python-pdal
  - gdal
  - gcc
  - cxx-compiler
  - libgcc=5.2.0
  - shapely  # needed to install the correct version of geos
  - pip
  - pip:
      - -r requirements.txt
      - .
